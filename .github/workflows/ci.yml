# CI pipeline — configured in Phase 1
# Placeholder to establish the file location and structure

name: CI

on:
  push:
    branches: ['**']
  pull_request:
    branches: [main]

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Phase 0 placeholder
        run: echo "CI pipeline will be configured in Phase 1"
